  {/* {tracks !== undefined &&
        tracks.map((track) => {
            return (
                <div>
                    <p>Música {track.name}</p> 
                    de {track.artists.map((trackArtist) => {
                        console.log(trackArtist)
                        return (
                        <p>{trackArtist.name}</p>
                        )
                })}
            </div>
            )    
        })} */}

        
        // .map((trackArtist) => {
        //     return <span>{trackArtist.name} </span>;
        //     })

        // .map((trackArtist) => {
        //     return <span>{trackArtist.name} </span>;
        //   })